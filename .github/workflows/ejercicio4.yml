name: Ejercicio 4 - Matrix Strategy Multi-OS

on:
  push:
    branches: [ main ]

jobs:
  test-multi-os:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    
    runs-on: ${{ matrix.os }}
    
    steps:
      # Paso 1: Hacer checkout del código
      - name: Checkout del código
        uses: actions/checkout@v3
      
      # Paso 2: Configurar Node.js versión 18
      - name: Configurar Node.js 18
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      # Paso 3: Ejecutar npm test
      - name: Ejecutar npm test
        run: |
          echo "Ejecutando tests..."
          # Simular tests para el ejercicio
      
      # Paso 4: Imprimir el nombre del sistema operativo 
      - name: Imprimir sistema operativo
        run: |
          echo "RUNNER_OS: $RUNNER_OS"
          echo "Matrix OS: ${{ matrix.os }}"
      
      # Paso 5: Step adicional SOLO para Ubuntu
      - name: Solo en Linux
        if: runner.os == 'Linux'
        run: echo "Este step solo aparece en Linux"
